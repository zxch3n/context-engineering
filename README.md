# context-engineering

Reusable skills for context engineering workflows.

## Layout

```text
skills/
  <skill-name>/
    SKILL.md
    agents/
      openai.yaml
```

Each skill is self-contained and should follow the shared agent skills shape used by repositories such as `vercel-labs/skills`.
